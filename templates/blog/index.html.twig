{% extends 'base.html.twig' %}

{% block title %}Blog!
{% endblock %}

{% block body %}

	<section class="container">

		<div
			class="row g-5">
			{# <div class="col-md-8"> #}
			<h1 class="pb-4 mb-4 fst-italic border-bottom">Blog</h1>
			{% for articleBlogs in articleBlog %}

			</section>
			<div class="d-flex justify-content-center">
				<img src="{{ vich_uploader_asset(articleBlogs, 'imageFile') }}" alt="{{articleBlogs.image}}" class="rounded mt-5 w-100 " style="height: 450px;">
			</div>
			<h2 class="display-6 fw-bold text-center my-4">{{articleBlogs.title}}</h2>
			<div class="col-lg-7 mx-auto">
				<p>{{articleBlogs.content | raw}}
				</p>
				<hr class="my-4">
				<p>Publié par:
					<a href="#">{{articleBlogs.author}}</a>
					le:
					{{articleBlogs.createdAt? articleBlogs.createdAt|date('d/m/Y') : '' }}
				</p>
				<!-- ajouter à la place du hashtag du href la route vers le profil utilisateur-->
			</div>
		{% endfor %}
		{# {% for blog in blogs %}
							<article class="blog-post">
								<h2 class="blog-post-title mb-1">{{blog.title}}</h2>
								
								{% if blog.image %}
									<a href="#">
										<img src="{{ vich_uploader_asset(blog, 'imageFile') }}" alt="{{blog.image}}" class="rounded-t-lg">
									</a>
		
									<a href="#">
										<img src="{{ vich_uploader_asset(blog, 'imageFile')}}" alt="{{blog.image}}" class="rounded-t-lg">
									</a>
									<p>{{blog.author}}</p>
									<p class="blog-post-meta">{{blog.createdAt? blog.createdAt|date('d/m/Y') : '' }}
										<a href="#">Mark</a>
									</p>
										<hr>
									<p>{{blog.content}}</p>
								{% endif %}
							</article>
						{% endfor %} #}
	</div>

</section>{% endblock %}

{% extends 'users.html.twig' %}

{% block title %}moness- Modification de l'utilisateur
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-4">Formulaire de modification des informations de l'utilisateur</h1>


		{% for flashMessage in app.flashes('warning') %}
			<div class="alert alert-warning">
				{{ flashMessage }}
			</div>
		{% endfor %}

		{{form(form)}}
		</div>
	{% endblock %}
